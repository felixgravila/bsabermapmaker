from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='ML project to generate a beat map from audio files',
    author='Felix Gravila',
    license='MIT',
)
